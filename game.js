var numberOfFaces = 0;
var score = 0;
var theLeftSide = document.getElementById("leftSide");
var count = 0;
function generateFaces() {
    
    img.src="images/smile.png";
    if (score <= 0) {
        numberOfFaces = 5;
    }
    else if (score == score / 5) {
        numberOfFaces = score / 5;
    }
    while (count <= numberOfFaces) {
        document.createElement("img");
        
    }
    
}